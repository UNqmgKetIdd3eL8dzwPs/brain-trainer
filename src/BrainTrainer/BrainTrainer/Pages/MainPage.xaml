<?xml version="1.0" encoding="utf-8"?>

<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:pages="clr-namespace:BrainTrainer.Pages;assembly=BrainTrainer"
                       x:Class="BrainTrainer.Pages.MainPage" BackgroundColor="#F0F1F2">
    <ListView ItemsSource="{Binding Questions}" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding QuestionText}" TextColor="#293949" />
                        <BoxView HeightRequest="1" HorizontalOptions="Fill" Color="Gray" />
                        <Label Text="{Binding Answer}" TextColor="#293949" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</pages:BaseContentPage>