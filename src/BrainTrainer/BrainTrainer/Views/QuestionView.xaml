<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:BrainTrainer.Controls;assembly=BrainTrainer"
             x:Class="BrainTrainer.Views.QuestionView">
    <controls:CustomScrollView Orientation="Vertical">
        <StackLayout x:Name="StackLayout">
            <Image x:Name="QuestionImage" IsVisible="{Binding HasImage}" HorizontalOptions="Fill"
                   Source="{Binding Image}"
                   Aspect="AspectFit" PropertyChanged="BindableObject_OnPropertyChanged" />
            <Label Text="{Binding QuestionText}" VerticalOptions="Fill" TextColor="{StaticResource TextColorDark}" FontSize="30" />
        </StackLayout>
    </controls:CustomScrollView>
</ContentView>